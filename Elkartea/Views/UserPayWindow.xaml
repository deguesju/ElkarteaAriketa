<UserControl x:Class="TPV_Gastronomico.Views.UserPayWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="650" Height="600">

	<Grid Background="WhiteSmoke" Margin="10">

		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<!-- SEARCH PANEL -->
		<StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="0,0,0,10">
			<TextBox x:Name="txtSearch" Width="220" Height="34" Margin="0,0,8,0"/>
			<Button Content="Bilatu" Height="34" Width="80"
                    Background="#3E3E42" Foreground="White" BorderBrush="Transparent"/>
		</StackPanel>

		<!-- SEARCH RESULTS LIST -->
		<ListBox x:Name="lbResults"
                 Grid.Row="1"
                 Margin="0,0,0,10"
                 Height="150"
                 Visibility="Collapsed"
                 MouseDoubleClick="lbResults_MouseDoubleClick">

			<ListBox.ItemTemplate>
				<DataTemplate>
					<Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="4" Padding="6" Margin="4">
						<StackPanel Orientation="Horizontal">
							<Image Source="{Binding ImagePath}" Width="56" Height="56" Margin="0,0,8,0"/>
							<StackPanel>
								<TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14"/>
								<TextBlock Text="{Binding Price, StringFormat=Precio: {0:C}}"/>
							</StackPanel>
						</StackPanel>
					</Border>
				</DataTemplate>
			</ListBox.ItemTemplate>

		</ListBox>

		<!-- DATAGRID -->
		<DataGrid x:Name="dgConsumed"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  Background="White"
                  ColumnHeaderHeight="32"
                  RowHeight="28">

			<DataGrid.Columns>
				<DataGridTextColumn Header="Produktua" Binding="{Binding Product}" Width="*"/>
				<DataGridTextColumn Header="Kant." Binding="{Binding Quantity}" Width="60"/>
				<DataGridTextColumn Header="Prezioa" Binding="{Binding UnitPrice, StringFormat=C}" Width="90"/>
				<DataGridTextColumn Header="Subtotal" Binding="{Binding Subtotal, StringFormat=C}" Width="90"/>
			</DataGrid.Columns>
		</DataGrid>


		<!-- FOOTER -->
		<Border Grid.Row="3" Background="White" Padding="10" CornerRadius="6" Margin="0,10,0,0">
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="*"/>
					<ColumnDefinition Width="230"/>
				</Grid.ColumnDefinitions> 

				<!-- Add / Remove buttons -->
				<StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
					<Button Content="Gehitu" Width="80" Height="34" Background="#3E3E42" Foreground="White"/>
					<Button Content="Kendu" Width="80" Height="34" Margin="8,0,0,0"
                            Background="#3E3E42" Foreground="White"/>
				</StackPanel>

				<!-- TOTAL + PAY -->
				<StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center">
					<StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
						<TextBlock Text="Totala:" FontSize="16" FontWeight="Bold" Margin="0,0,6,0"/>
						<TextBlock x:Name="txtTotal" Text="EUR 0.00" FontSize="16" FontWeight="Bold"/>
					</StackPanel>

					<StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
						<Button Content="Utzi" Width="90" Height="34" Margin="0,0,6,0"
                                Background="#777" Foreground="White"/>
						<Button Content="Ordaindu" Width="100" Height="34"
                                Background="#1E90FF" Foreground="White"/>
					</StackPanel>
				</StackPanel>

			</Grid>
		</Border>
	</Grid>
</UserControl>
